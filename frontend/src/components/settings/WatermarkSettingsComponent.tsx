/**
 * WatermarkSettingsComponent - 向后兼容导出
 * 重新导出重构后的组件以保持向后兼容性
 */

import { WatermarkSettingsComponent } from './WatermarkSettingsComponents';

export { WatermarkSettingsComponent };
export default WatermarkSettingsComponent;
