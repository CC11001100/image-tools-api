apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-tools-api-backend
  namespace: aigchub-prod
  labels:
    app: image-tools-api-backend
    component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: image-tools-api-backend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: image-tools-api-backend
        component: backend
    spec:
      # 使用 hostNetwork 模式，让Pod使用服务器IP
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      
      # 使用 Harbor 镜像仓库的认证
      imagePullSecrets:
        - name: harbor-secret
      
      containers:
        - name: backend
          # 局域网集群使用 192.168.3.42:5000 镜像仓库
          image: 192.168.3.42:5000/aigchub/image-tools-api-backend:latest
          imagePullPolicy: Always
          
          ports:
            - name: http
              containerPort: 58888
              protocol: TCP
          
          env:
            - name: TZ
              value: "Asia/Shanghai"
            - name: PYTHONUNBUFFERED
              value: "1"
          
          # 健康检查 - hostNetwork模式下使用localhost
          # 增加检查间隔以减少CPU占用和日志量
          livenessProbe:
            httpGet:
              path: /api/health
              port: 58888
              host: 127.0.0.1
            initialDelaySeconds: 30
            periodSeconds: 60
            timeoutSeconds: 10
            failureThreshold: 3
          
          readinessProbe:
            httpGet:
              path: /api/health
              port: 58888
              host: 127.0.0.1
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          
          # 资源限制 - 根据实际使用优化
          resources:
            requests:
              memory: "80Mi"
              cpu: "5m"
            limits:
              memory: "128Mi"
              cpu: "50m"
          
          # 挂载目录
          volumeMounts:
            - name: uploads
              mountPath: /app/uploads
            - name: temp
              mountPath: /app/temp
            - name: logs
              mountPath: /app/logs
            - name: localtime
              mountPath: /etc/localtime
              readOnly: true
      
      volumes:
        - name: uploads
          emptyDir: {}
        - name: temp
          emptyDir: {}
        - name: logs
          emptyDir: {}
        - name: localtime
          hostPath:
            path: /etc/localtime
            type: File
      
      # 重启策略
      restartPolicy: Always
